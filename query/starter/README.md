# jtenman-query-starter

Spring Boot auto-configuration wiring jtenman's own read side into a deployable - not the one an administered application adds, which is [`jtenman-starter`](../../starter/README.md).
